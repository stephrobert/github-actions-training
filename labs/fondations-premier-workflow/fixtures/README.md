# calculator

A small Python library used as a building block by other projects. Four
functions, nine pytest tests.

```bash
python -m pip install -r requirements.txt
python -m pytest
```

The tests pass when somebody remembers to run them. Nobody does.
